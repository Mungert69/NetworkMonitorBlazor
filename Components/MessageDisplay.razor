@using NetworkMonitorBlazor.Models

<div class="message @(Message.Success ? "success" : !string.IsNullOrEmpty(Message.Info) ? "info" : "error")">
    @Message.Text
</div>

@code {
    [Parameter, EditorRequired]
    public ChatMessage Message { get; set; } = new();
}